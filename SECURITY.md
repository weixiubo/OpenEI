# 安全说明

OpenEI 会把模型输出转成机器人动作，因此安全边界必须放在运行时和适配器层，而不是只依赖提示词。

## 基本原则

- 高风险任务需要确认或直接拒绝。
- 适配器必须提供停止能力。
- 技能包视为可执行能力声明，启用前应审查。
- 不在仓库提交真实 API key、token、串口日志或用户数据。

## 报告问题

如果发现安全问题，请先私下联系仓库维护者，不要直接公开可复现攻击细节。
